import 'package:flutter/material.dart';

class HighRatedDoctors extends StatelessWidget {
  const HighRatedDoctors({super.key});

  @override
  Widget build(BuildContext context) {
    var size = MediaQuery.of(context).size;
    return Container();
  }
}
